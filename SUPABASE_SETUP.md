# تعليمات إعداد المزامنة التلقائية مع Supabase

## الخطوة 1: إنشاء حساب Supabase

1. اذهب إلى [Supabase](https://supabase.com/)
2. اضغط على "Start your project" أو "Sign up"
3. سجّل حساب جديد (يمكن استخدام GitHub)

## الخطوة 2: إنشاء مشروع جديد

1. من لوحة التحكم، اضغط على "New project"
2. أدخل:
   - **Name**: اسم المشروع (مثلاً: debt-management)
   - **Database Password**: كلمة مرور قوية لقاعدة البيانات
   - **Region**: اختر أقرب منطقة لك
3. اضغط على "Create new project"
4. انتظر حتى يكتمل إنشاء المشروع (قد يستغرق دقيقة أو دقيقتين)

## الخطوة 3: الحصول على إعدادات المشروع

1. من لوحة التحكم، اضغط على ⚙️ (Settings) → "API"
2. انسخ:
   - **Project URL** (مثل: `https://xxxxx.supabase.co`)
   - **anon public** key (مفتاح public)

## الخطوة 4: إنشاء الجدول في قاعدة البيانات

1. من القائمة الجانبية، اضغط على "Table Editor"
2. اضغط على "New table"
3. أدخل:
   - **Name**: `clients_data`
   - **Description**: "بيانات العملاء للمزامنة"
4. أضف الأعمدة التالية:

   | Column name | Type | Default value | Nullable | Unique |
   |-------------|------|---------------|----------|--------|
   | id | text | - | ❌ | ✅ |
   | clients | jsonb | - | ❌ | ❌ |
   | last_updated | timestamptz | now() | ❌ | ❌ |
   | updated_by | text | - | ✅ | ❌ |

5. اضغط على "Save"
6. أضف صف أولي:
   - **id**: `main`
   - **clients**: `[]` (مصفوفة فارغة)
   - اضغط على "Insert row"

## الخطوة 5: تفعيل Realtime (للمزامنة الفورية)

1. من Table Editor، اضغط على الجدول `clients_data`
2. اضغط على "Replication" أو "Realtime"
3. فعّل "Enable Realtime" للجدول
4. اضغط على "Save"

## الخطوة 6: تحديث ملف supabase-config.js

افتح ملف `supabase-config.js` واستبدل:

```javascript
const SUPABASE_URL = "ضع_Project_URL_هنا";
const SUPABASE_ANON_KEY = "ضع_anon_key_هنا";
```

مثال:
```javascript
const SUPABASE_URL = "https://xxxxx.supabase.co";
const SUPABASE_ANON_KEY = "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...";
```

## الخطوة 7: إعداد Row Level Security (RLS)

1. من Table Editor → `clients_data` → "Policies"
2. اضغط على "New Policy"
3. اختر "Create policy from scratch"
4. أدخل:
   - **Policy name**: `Enable all access`
   - **Allowed operation**: ALL
   - **Policy definition**: `true` (للفترة الحالية - يمكن تقييدها لاحقاً)
5. اضغط على "Review" ثم "Save policy"

## الخطوة 8: اختبار المزامنة

1. افتح الموقع في متصفحين مختلفين أو جهازين مختلفين
2. سجّل الدخول في كليهما
3. أضف عميل في المتصفح الأول
4. ستظهر البيانات تلقائياً في المتصفح الثاني خلال ثوانٍ!

## ملاحظات مهمة:

- ✅ البيانات تُحفظ محلياً أولاً (للاستمرار في العمل بدون إنترنت)
- ✅ ثم تُرفع تلقائياً لـ Supabase
- ✅ أي تغيير في أي جهاز يظهر على جميع الأجهزة فوراً
- ✅ النسخ المحلية تعمل كنسخ احتياطية

## الأمان:

لزيادة الأمان لاحقاً، يمكنك:
1. إضافة نظام المصادقة في Supabase
2. تحديث RLS policies لتقييد الوصول
3. تشفير البيانات قبل رفعها

## الأسعار:

- **الخطة المجانية**: 500 MB قاعدة بيانات، 2 GB bandwidth شهرياً (كافية للبداية)
- **الخطة Pro**: $25/شهر (للاستخدام الأكبر)

---

**بعد إكمال الإعداد، ستعمل المزامنة التلقائية بين جميع الأجهزة! 🎉**

